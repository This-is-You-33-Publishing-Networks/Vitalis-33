// Vitalis — Arrays
//
// Demonstrates array literals and indexing.

fn main() -> i64 {
    let values = [10, 20, 30, 40, 50];
    let first = values[0];     // 10
    let third = values[2];     // 30
    let last = values[4];      // 50
    first + third + last       // 90
}
